---
import Container from "@/components/Container.astro";

interface Props {
  eyebrow?: string;
  title: string;
  description?: string;
}

const { eyebrow, title, description } = Astro.props;
---

<Container as="section" class="pb-12 pt-16 sm:pb-16 sm:pt-24">
  {eyebrow && <p class="section-kicker mb-4 text-accent">{eyebrow}</p>}
  <h1 class="max-w-3xl text-4xl font-medium tracking-tight text-text sm:text-5xl" style="font-family: 'Playfair Display', 'Noto Serif SC', Georgia, serif;">
    {title}
  </h1>
  {
    description && (
      <p class="mt-6 max-w-2xl text-lg leading-8 text-text-muted">
        {description}
      </p>
    )
  }
</Container>
