---
interface Props {
  eyebrow?: string;
  title: string;
  description?: string;
  actionHref?: string;
  actionLabel?: string;
}

const { eyebrow, title, description, actionHref, actionLabel } = Astro.props;
---

<div class="mb-8 flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between">
  <div>
    {eyebrow && <p class="section-kicker mb-3">{eyebrow}</p>}
    <h2 class="text-2xl font-bold tracking-tight text-text sm:text-3xl">{title}</h2>
    {description && <p class="mt-4 max-w-2xl leading-7 text-text-muted">{description}</p>}
  </div>
  {
    actionHref && actionLabel && (
      <a href={actionHref} class="text-link focus-ring rounded-md text-sm whitespace-nowrap">
        {actionLabel} →
      </a>
    )
  }
</div>
